{
  "name": "@agentkit/channel-http",
  "version": "0.0.1",
  "description": "HTTP plugin — REST API + SSE → EventLoop events",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "vitest run" },
  "dependencies": {
    "@agentkit/agentic": "workspace:*",
    "@agentkit/event-loop": "workspace:*"
  },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.7.0" }
}
