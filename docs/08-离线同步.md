# 离线消息同步

## 1. 同步策略

| 场景 | 处理方式 |
|------|------|
| 正常在线 | 实时通过 WebSocket 接收 |
| 短暂断线重连 | 重连后发送上次消息时间戳，拉取缺失消息 |
| 长时间离线 | 按时间戳拉取，最多 200 条 |
| 新设备登录 | 向 Agent 请求历史记录（存储在用户自己的 OpenClaw） |

## 2. 多设备处理

广播方案：Agent 消息同时推给该房间所有在线 Client（手机 + 电脑同时收到），体验与微信一致。
